# edu tmux config

My tmux config. That's it.

## Install
Either copy to your `$HOME` folder, or link, e.g.

```
cd
ln -s <path to this repo>/.tmux.conf
```

and install the [Tmux Plugin Manager](https://github.com/tmux-plugins/tpm).

Or run 

```
sh <path to this repo>/install.sh
```
